#include<stdio.h>
int main()
{
int num,divisor=2,a;
scanf("%d",&num);
int no_of_1s=0;
while(num>0)
{     
    a=(num-(num / divisor)*divisor);
if(a==1)
    {
        no_of_1s++;
    }
    num=num/2;
}
   printf("%d",no_of_1s);
   
}
